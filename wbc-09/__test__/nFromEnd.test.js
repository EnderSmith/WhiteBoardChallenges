'use strict';

const nFromEnd = require('../lib');

require('jest');

describe('nFromEnd', () => {
  describe('happy case', () => {
    it('should return value of the nth node from the end', () => {
      expect(nFromEnd(3, ))
    });
  });
});
